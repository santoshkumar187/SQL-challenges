🏆 Second Highest Salary
Difficulty: Easy Concepts: SELECT, ORDER BY, LIMIT, DISTINCT, SUBQUERY

📄 Problem Description:
You're given a table called Employee with the following schema:

sql
Employee
+-------------+--------+
| Id          | Salary |
+-------------+--------+
| 1           | 100    |
| 2           | 200    |
| 3           | 300    |
+-------------+--------+
Write a SQL query to get the second highest salary from the Employee table. If there is no second highest salary, the query should return null.

💡 Sample Solution:
sql
SELECT MAX(Salary) AS SecondHighestSalary
FROM Employee
WHERE Salary < (
    SELECT MAX(Salary) FROM Employee
);


✅ Final Output
SecondHighestSalary
200
